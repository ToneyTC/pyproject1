# 修改日志

## 2026-05-09 16:37:55

- **文件**：`playwright_demo.py`
- **说明**：
  - 修正文件首行 `from playwright.sync_api import sync_playwright` 的错误缩进，避免语法错误。
  - 在「更新计划」流程中，待审核（`_待审核依次审核`）结束后自动进入 **工艺管理**（生产管理 → 工艺 → 工艺管理），可选点击「启用中」页签。
  - 按 Excel 数字代码列表依次在「工艺」筛选框查询，在结果表格中选取 **不包含半成品/BCP** 的首行（沿用 `_行包含半成品或bcp` 逻辑），并点击该行中的 **工艺编号**（优先匹配编码形态的单元格与链接）。
- **新建**：本 `log.md` 用于记录变更。

## 2026-05-09 16:43:49

- **文件**：`playwright_demo.py`
- **说明**：修复进入工艺管理时 `Page.click("b.quick-name:text('生产管理')")` 超时。审核结束后页面常在「工艺」子菜单下，一级「生产管理」可能不可见或选择器失效。
  - 抽出 `_启用中页签()`；新增 `_侧边点击一级菜单()`，对 `b.quick-name` / `.el-submenu__title` / `.el-menu-item` / `.sidebar-container .el-menu` 做兜底。
  - `_进入工艺管理()` 改为：**优先**直接点击 `div.child-menu-name:工艺管理`（`force` + 滚入视口）；失败则先点 `span.title:工艺` 展开；仍失败再点一级「生产管理」后点「工艺」「工艺管理」。超时改为 **15s**。

## 2026-05-09 16:50:15

- **文件**：`playwright_demo.py`
- **说明**：修复工艺管理步骤点击筛选框超时。
  - **原因**：`_工艺搜索输入框()` 回退到 `#container input.el-input__inner` 第一个，在产品 BOM 页命中「请输入物料代码」；同时 **确认信息** `el-message-box__wrapper` 遮挡点击。
  - **改动**：新增 `_关闭确认信息弹窗()`；`_解析工艺筛选输入框()` 仅在「工艺」表单项下选取 input，并排除 `placeholder` 含「物料代码」；去掉危险回退；`_等待工艺管理页就绪()` 用于确认已进入工艺管理。
  - `_进入工艺管理()` 在点击菜单前后关闭弹窗，并以筛选区就绪为准，失败则再走一遍完整路径并 `raise` 明确错误。
  - `_工艺管理依次搜索并点击工艺编号()` 每条代码前关闭弹窗、重新解析筛选框与查询按钮；点击失败时 `force=True` 兜底。

## 2026-05-09 17:08:01

- **文件**：`playwright_demo.py`
- **说明**：点击工艺编号进入「工艺基本信息」详情后，自动校验三项下拉展示是否与期望一致：
  - 批记录报表 → `RC-9000-05-02 批包装记录（2025.10.26）`
  - 指令报表 → `RC-9001-15-03 中药饮片批包装指令（2025.10.26）`
  - 指令参数记录 → `饮片包装批指令-新`
  - 含 `_展示值匹配期望()` 宽松匹配（界面截断省略号时比对前缀与去空白）。
  - 校验后 `_返回工艺管理列表页()`（面包屑「工艺管理」或 `go_back`），以便 Excel 多代码循环继续查询。

## 2026-05-09 17:26:27

- **文件**：`playwright_demo.py`
- **说明**：工艺详情页在三项报表校验之后增加 `_校验加工顺序并点击包装再验工序记录报表()`：
  - 采集带「数字、」前缀的步骤（`li` / `el-menu-item`，必要时从「加工顺序」标题祖先卡片内收集），要求 **恰好 1 条** 且文案含 **「包装」**；
  - 满足则点击「数字、包装」项；再切「基本信息」页签（若存在），读取 **工序记录报表**，与 `RC-9020-01-11包装生产记录（2026.01.26）` 做 `_展示值匹配期望` 比对；
  - 加工顺序不满足或未成功点击包装时 **跳过** 工序记录报表校验并打印说明。
- **依赖**：模块级增加 `import re`。

## 2026-05-09 17:31:17

- **文件**：`playwright_demo.py`
- **说明**：修复「加工顺序」采集为 0 条的问题。
  - 优先在页面内用 **`evaluate`** 扫描 `#container` 下 `div/span/li/p/a/button/td` 的 **单行** `^\d+\u3001\S+` 文案（长度≤100），按文本长度排序去重，适配非 `li`/`.el-menu-item` 结构的步骤列表。
  - Python 侧步骤正则改为 `\d+\s*[\u3001、]\s*\S`；Playwright 兜底选择器扩展为 tree/timeline/step/order/sequence/cell 等。
  - 「加工顺序」标题的祖先面板增加 `panel`/`aside` 及浅层 `div` 的 XPath 兜底。
  - 点击优先 **`root.get_by_text(only, exact=True)`**，再宽匹配 `\d+[\u3001、]包装`。

## 2026-05-10 16:02:47

- **文件**：`playwright_demo.py`
- **说明**：修复工艺详情页「加工顺序」校验始终 **收集 0 条** 的问题。
  - **原因**：`_加工顺序文案_eval` 仅在 `#container` 内扫描，而「加工顺序」步骤列表常在侧栏/主布局其它区域，不在 `#container` DOM 子树内；Playwright 兜底也用 `#container` 为根的 `locator(broad)`，同样漏扫。
  - **改动**：浏览器内改为以 **`document.body`** 为主扫描；若无命中再在文案为「加工顺序」的标签向上遍历祖先 **12 层** 内收窄扫描。序号与工序名之间的分隔符在 JS 与 Python 侧一并支持 **`、` / `,` / `，` / `.` / `．`（U+FF0E）**。Playwright  broad 兜底改为以 **整页** `page.locator(broad)` 过滤；点击「包装」步的正则同步放宽。

## 2026-05-10 16:05:13

- **文件**：`playwright_demo.py`
- **说明**：按页面实测 DOM 增加「加工顺序」步骤列表的 **XPath 优先采集**：
  - 精确：`//*[@id='container']/div/div/div/div[2]/div/div[2]/div[1]/div[3]/ul/span/li`
  - 兜底：`//*[@id='container']//ul/span/li`（同一结构在不同布局版本时中间 div 层级可能变化）
  - 命中且文案符合步骤正则时，点击对应 **`li`** 采用 `filter(has_text=only)`，再走原有 `get_by_text` 等兜底。

## 2026-05-10 16:19:29

- **文件**：`playwright_demo.py`
- **说明**：修复「加工顺序」采集到 **216 条** 的严重误报。
  - **原因**：宽松 XPath `//*[@id='container']//ul/span/li` 命中 **全容器** 内大量下拉、待办等 `li`；步骤正则使用 **`search()`**，长文案中的 **`1.0`**（如规格 **1.0kg**）也会被当成「序号+句点」步骤。
  - **改动**：**删除**上述宽松 XPath，仅保留用户提供的 **精确** `ul/span/li` 路径。步骤文案改为 **`pattern_step_line` 整行锚定**（`match` + 序号 1–4 位 + 长度 ≤64），Playwright 粗筛单独用 `pattern_step_hint`；浏览器内 `evaluate` 同步改为 **首尾锚定** 与长度上限。

## 2026-05-10 16:25:16

- **文件**：`playwright_demo.py`
- **说明**：适配工艺详情页工序列表 DOM：**`li.working-procedure-item`**，序号在 **`b.index`**、名称在 **`div`**（如「包装」），整段 `inner_text` 未必含「1、」。
  - **采集**：优先 `#container` / 全页的 **`li.working-procedure-item`**，用 **`_读取工序列表项文案`** 合并为 **`{序号}、{名称}`**；若只有短中文名称（序号未读出），仅在该列表上用 **`_是工序项纯名称兜底`** 接受（如「包装」）。
  - **正则**：`pattern_step_line` 增加 **`1 包装`**、**`1包装`** 等形式；`evaluate` 内先 **`collectWorkingProcedure`**（同源逻辑 + 纯名称），再全盘扫描。
  - **点击**：增加 **`li.working-procedure-item` + `包装`** 兜底；分隔点击正则支持 **`1包装`**。

## 2026-05-10 17:05:39

- **文件**：`playwright_demo.py`
- **说明**：工艺管理详情校验失败时的自动补救：
  - 若 **批记录报表 / 指令报表 / 指令参数记录** 任一不符（`ok3` 为假）、或 **加工顺序（含工序记录报表）** 不符（`ok_step` 为假）、或 **未检测到工艺详情表单**，则 **`_返回工艺管理列表页()`** → 按进入详情前的 **`target_row_index`** 重新取表行 → **`_勾选表格行首列`** → **`_工艺管理点击工艺升级()`**。
  - 抽出 **`_勾选表格行首列`**，**`_勾选并点击编辑`** 改为先勾选再点编辑；新增 **`_工艺管理点击工艺升级`**（`#container` 内 `button/span` 含「工艺升级」及全页按钮兜底）。

## 2026-05-10 17:07:14

- **文件**：`playwright_demo.py`
- **说明**：根据工艺管理页截图（筛选区下方横向工具栏），**`_工艺管理点击工艺升级`** 优先 **`#container`** 内 **`get_by_role('button', name='工艺升级')`**，其次 **`button.el-button` / `.el-button`** 的 **`filter(has_text='工艺升级')`**，再兜底全页同名按钮；去掉单独点 **`span`**（避免点到非按钮节点）。
